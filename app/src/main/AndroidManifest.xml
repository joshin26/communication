<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewAPP">
        <activity android:name=".Communicate"></activity>
        <activity android:name=".Log_account_creation_three" />
        <activity android:name=".Log_account_creation_two" />
        <activity android:name=".Log_account_creation" />
        <activity android:name=".Log_forget_password_two" />
        <activity android:name=".Login" />
        <activity android:name=".Log_forget_password" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>